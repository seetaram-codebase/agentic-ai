# Lambda Chunker Dependencies - Ultra Minimal
# Only what's absolutely necessary for chunking

# AWS SDK (already included in Lambda runtime, but specify for local dev)
boto3>=1.34.0

# LangChain - ONLY what we need for document loading and splitting
langchain-community==0.3.12
langchain-text-splitters==0.3.2

# PDF Processing
pypdf==5.4.0

# Tiktoken for token-based chunking
tiktoken>=0.5.0

